//un arreglo es una variable que puede contener uno o varios elementos

// declaracion de arreglos

const numeros = [1,2,3,4,5,6];
console.log(numeros);


//declarar un arreglo con el constructor
const meses = new Array('enero', 'febrero', 'marzo', 'abril', 'mayo', 'junio');
console.log(meses);

const deTodo = [true, "string", 4, null, {nombre: 'martin'}];
console.log(deTodo);